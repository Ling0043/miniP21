% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_missing_cond.R
\name{check_missing_cond}
\alias{check_missing_cond}
\title{Check Required Value Under Conditions}
\usage{
check_missing_cond(
  df,
  domain_name,
  target_vars,
  cond_vars,
  cond_ops,
  cond_vals,
  logic_op = "AND",
  rule_id,
  ...
)
}
\arguments{
\item{df}{A data frame containing the domain data to check.}

\item{domain_name}{Character string. Name of the domain (e.g., \code{"DM"}).}

\item{target_vars}{Character string. Name of the variable that must not be
missing when the conditions hold.}

\item{cond_vars}{Character vector. Names of the condition columns. Length
must equal length of \code{cond_ops} and \code{cond_vals}.}

\item{cond_ops}{Character vector. Operators to apply to each condition
column. Allowed values: \code{"non_missing"} (not NA and not empty string),
\code{"missing"} (NA or empty string), \code{"equal"} (exact match, case‑sensitive),
\code{"not_in"} (value not in a comma‑separated list; the token \verb{__MISSING__}
inside the list excludes missing values), \code{"greater_than"} (numeric
comparison, NA is treated as not satisfying).}

\item{cond_vals}{Character vector. Values corresponding to each operator.
Ignored for \code{"non_missing"} and \code{"missing"} (use \code{""} as placeholder).
For \code{"equal"} a single string, for \code{"not_in"} a comma‑separated list
(e.g. \code{"A,B,__MISSING__"}), for \code{"greater_than"} a numeric string.}

\item{logic_op}{Character string. Logical operator to combine multiple
conditions. Either \code{"AND"} (default) or \code{"OR"}.}

\item{rule_id}{Character string. Identifier of the validation rule.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A data frame with columns \code{row_number}, \code{variable_name},
\code{original_value}, \code{rule_id}, and \code{error_message} for each record where
the target variable is missing despite the conditions being met, or
\code{NULL} if no violations or required columns are missing.
}
\description{
Verifies that a target variable is not missing when one or more
condition variables satisfy specified operators. Supports conditions of
type “non‑missing”, “missing”, “equal to a value”, “not in a set of values”,
and “greater than a numeric threshold”. Conditions can be combined using
AND (default) or OR logic.
}
\details{
Implements SDTM validation rules where a variable must be populated
depending on other variables’ states. Applicable rules:
SD0016, SD0024, SD0026, SD0027, SD0029, SD0030, SD0032, SD0033, SD0034,
SD0035, SD0036, SD0043, SD0044, SD0045, SD0049, SD0050, SD0087, SD0092,
SD1036, SD1037, SD1065, SD1098, SD1131, SD1209, SD1213, SD1241, SD1265,
SD1268, SD1289, SD1291, SD1292, SD1313, SD1320, SD1342, SD1343, SD1350,
SD1351, SD1369, SD1370, SD1372, SD1454, SD1461, SD1465, SD2005, SD2023,
SD2286.
}
\author{
Zhu Xiuling
}
