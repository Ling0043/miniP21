% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_ts_param_required.R
\name{check_ts_param_required}
\alias{check_ts_param_required}
\title{Check Required Trial Summary Parameter}
\usage{
check_ts_param_required(
  df,
  target_vars,
  param_code,
  cond_param = NULL,
  cond_val = NULL,
  val_var = "TSVAL",
  rule_id,
  ...
)
}
\arguments{
\item{df}{A data frame containing the domain data to check.}

\item{target_vars}{Character string. Name of the column containing parameter
codes (e.g., "TSPARMCD").}

\item{param_code}{Character string. The specific parameter code that must
be populated (e.g., "FCNTRY").}

\item{cond_param}{Character string. Optional. Name of the condition parameter
code (e.g., "STYPE") that triggers the check. Default is NULL.}

\item{cond_val}{Vector of values. Optional. Values in \code{cond_param} that
trigger the check (e.g., "INTERVENTIONAL"). Default is NULL.}

\item{val_var}{Character string. Name of the column containing parameter
values (e.g., "TSVAL"). Default is "TSVAL".}

\item{rule_id}{Character string. Identifier of the validation rule (e.g.,
"SD2224").}

\item{...}{Additional arguments (currently unused).}
}
\value{
A data frame with columns \code{row_number}, \code{variable_name},
\code{original_value}, \code{rule_id}, and \code{error_message} for each missing
parameter, or \code{NULL} if no errors or required columns are missing.
}
\description{
Checks whether a specific parameter code exists and is populated
in a vertical data structure (e.g., Trial Summary domain). Supports
conditional checks where the parameter is only required if another
parameter equals a specific value. Used for rules SD2223 to SD2287.
}
\author{
Zhu Xiuling
}
