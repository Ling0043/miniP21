% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_be_null_cond.R
\name{check_be_null_cond}
\alias{check_be_null_cond}
\title{Check Prohibited (Must Be Missing) Under Conditions}
\usage{
check_be_null_cond(
  df,
  domain_name,
  target_vars,
  cond_vars,
  cond_ops,
  cond_vals,
  logic_op = "AND",
  rule_id,
  ...
)
}
\arguments{
\item{df}{A data frame containing the domain data to check.}

\item{domain_name}{Character string. Name of the domain to which the columns belong.}

\item{target_vars}{Character string. Name of the variable that must be
missing when the conditions are met.}

\item{cond_vars}{Character vector. Names of the condition columns. Length
must equal length of \code{cond_ops} and \code{cond_vals}.}

\item{cond_ops}{Character vector. Operators to apply to each condition
column. Allowed values: \code{"non_missing"} (not NA and not empty string),
\code{"missing"} (NA or empty string), \code{"equal"} (exact match, case‑sensitive),
\code{"not_in"} (value not in a comma‑separated list; \verb{__MISSING__} token
excludes missing values).}

\item{cond_vals}{Character vector. Values corresponding to each operator.
Ignored for \code{"non_missing"} and \code{"missing"} (use \code{""} as placeholder).
For \code{"equal"} a single string, for \code{"not_in"} a comma‑separated list
(e.g. \code{"A,B,__MISSING__"}).}

\item{logic_op}{Character string. Logical operator to combine multiple
conditions. Either \code{"AND"} (default) or \code{"OR"}.}

\item{rule_id}{Character string. Identifier of the validation rule.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A data frame with columns \code{row_number}, \code{variable_name},
\code{original_value}, \code{rule_id}, and \code{error_message} for each record where
the target variable is \strong{not} missing despite the conditions being met,
or \code{NULL} if no violations or required columns are missing.
}
\description{
Verifies that a target variable is missing (NA or empty string)
when one or more condition variables meet specified criteria. Supports
conditions of type “non‑missing”, “missing”, “equal to a value” and “not
in a set of values”. Conditions can be combined using AND (default) or OR
logic.
}
\details{
Implements SDTM validation rules where a variable must be blank
given the state of other variables. Applicable rules: SD1010, SD1013,
SD1019, SD1066, SD1067, SD1123, SD1137, SD1238, SD1251.
}
\author{
Zhu Xiuling
}
